export default function Footer() {
  return (
    <footer className="bg-slate-50 text-slate-600 text-center py-2 border-t border-slate-200 text-sm">
      © {new Date().getFullYear()} F-Dashboard (TMA). All rights reserved.
    </footer>

  );
}
